import React from "react";

const GoalList = ({ client_id }: { client_id: string }) => {
    console.log("GoalList", client_id);

    return (
        <div className="w-full h-40">
            {/* TODO: Implement Goal List for client {client_id} */}
        </div>
    );
};

export default GoalList;
