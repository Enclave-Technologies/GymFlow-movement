import React from "react";

const WorkoutHistory = ({ client_id }: { client_id: string }) => {
    console.log("WorkoutHistory", client_id);

    return (
        <div className="w-full h-40">
            {/* TODO: Implement Workout History for client {client_id} */}
        </div>
    );
};

export default WorkoutHistory;
